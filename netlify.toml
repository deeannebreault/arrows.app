[build]
  command = "npm run build"
  publish = "dist/apps/arrows-ts"

[build.environment]
  NODE_VERSION = "20"

# SPA routing: all paths serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
